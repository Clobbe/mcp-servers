# MCP Servers Project - AI Agent Guidelines

## Project Overview

This is a collection of Model Context Protocol (MCP) servers that provide specialized tools for development workflows. These servers work across Claude Code, OpenCode, Continue.dev, and other MCP-compatible clients.

## Critical Rules - MUST FOLLOW

### 1. Incremental Git Commits (MANDATORY)

**NEVER make one large commit. ALWAYS commit incrementally.**

Every logical change MUST be committed separately:

```bash
# ✅ CORRECT - Incremental commits
git add src/utils/types.ts
git commit -m "feat: add TypeScript type definitions"

git add src/utils/parser.ts
git commit -m "feat: add parser utility"

git add src/tools/new-tool.ts
git commit -m "feat: add new_tool implementation"

git add src/index.ts
git commit -m "feat: wire new_tool into MCP server"

git add README.md
git commit -m "docs: add new_tool usage examples"

# ❌ WRONG - One large commit
git add .
git commit -m "feat: add new feature"
```

**Commit Frequency Guidelines:**
- After adding each new file
- After implementing each function
- After wiring components together
- After updating documentation
- After fixing each bug
- Aim for 5-15 commits per feature, not 1

### 2. Conventional Commits (MANDATORY)

All commits MUST follow [Conventional Commits](https://www.conventionalcommits.org/) format:

```
<type>: <description>

[optional body]
```

**Types:**
- `feat:` - New features or tools
- `fix:` - Bug fixes
- `docs:` - Documentation only
- `refactor:` - Code restructuring without changing behavior
- `test:` - Adding or updating tests
- `chore:` - Maintenance tasks (dependencies, config, etc.)
- `perf:` - Performance improvements
- `style:` - Code formatting (not CSS)

**Examples:**
```
feat: add changelog_diff tool for comparing entries
fix: resolve symlink resolution on macOS
docs: add installation instructions for OpenCode
refactor: extract validation logic to separate module
chore: update @modelcontextprotocol/sdk to 0.7.0
```

### 3. Comprehensive Testing with Playwright (MANDATORY)

**Every feature MUST have tests. No exceptions.**

- Use Playwright as the testing framework
- Write tests BEFORE or DURING implementation (TDD encouraged)
- Test files go in `__tests__/` directory or as `*.test.ts` files
- Commit tests separately from implementation
- All tests must pass before merging to main

**Testing Requirements:**

```typescript
// __tests__/tool-name.test.ts
import { test, expect } from '@playwright/test';
import { myTool } from '../src/tools/my-tool.js';

test.describe('my_tool', () => {
  test('should handle valid input', async () => {
    const result = await myTool({ param: 'value' });
    expect(result.success).toBe(true);
  });

  test('should handle invalid input', async () => {
    const result = await myTool({ param: '' });
    expect(result.success).toBe(false);
    expect(result.message).toContain('Error');
  });

  test('should handle edge cases', async () => {
    // Test edge cases
  });
});
```

**Test Coverage Requirements:**
- Happy path (valid inputs)
- Error cases (invalid inputs)
- Edge cases (empty, null, undefined, extreme values)
- Integration tests (tool interaction with MCP server)
- At minimum 80% code coverage

**Testing Workflow:**

```bash
# 1. Write implementation
git add src/tools/new-tool.ts
git commit -m "feat: add new_tool implementation"

# 2. Write tests
git add __tests__/new-tool.test.ts
git commit -m "test: add comprehensive tests for new_tool"

# 3. Run tests
npm test

# 4. Fix any failures
git add src/tools/new-tool.ts
git commit -m "fix: handle edge case in new_tool"

# 5. Ensure all tests pass before proceeding
```

**Test File Naming:**
- `__tests__/tool-name.test.ts` for tool tests
- `__tests__/utils/helper.test.ts` for utility tests
- Mirror the `src/` structure in `__tests__/`

### 4. TypeScript Strict Mode (MANDATORY)

- Always use TypeScript strict mode
- No `any` types without explicit reason and comment
- Provide proper type definitions for all functions
- Export types that might be reused

### 5. MCP Server Structure (MANDATORY)

Each MCP server MUST follow this structure:

```
server-name/
├── README.md                 # Comprehensive documentation
├── package.json             # Node.js package config
├── tsconfig.json            # TypeScript config (strict mode)
├── playwright.config.ts     # Playwright test config
├── src/
│   ├── index.ts            # MCP server entry point
│   ├── tools/              # Individual tool implementations
│   │   └── tool-name.ts
│   └── utils/              # Shared utilities
│       ├── types.ts        # Type definitions
│       └── helpers.ts      # Helper functions
├── __tests__/              # Test files (mirrors src/)
│   ├── tools/
│   │   └── tool-name.test.ts
│   └── utils/
│       └── helpers.test.ts
└── build/                  # Compiled output (gitignored)
```

**Do NOT deviate from this structure.**

### 5. Error Handling (MANDATORY)

All MCP tools MUST:
- Use try-catch blocks
- Return user-friendly error messages
- Never expose internal stack traces to users
- Provide actionable error messages

```typescript
export async function myTool(args: Args): Promise<Result> {
  try {
    // Implementation
    return { success: true, message: 'Operation completed' };
  } catch (error) {
    return {
      success: false,
      message: `❌ Error: ${error instanceof Error ? error.message : String(error)}`,
    };
  }
}
```

### 6. Documentation (MANDATORY)

**Every change requires documentation updates:**

- Add JSDoc comments to all exported functions
- Update README.md when adding/changing tools
- Include usage examples
- Document all parameters and return values
- Keep documentation in sync with code

**Commit documentation separately:**
```bash
git add src/tools/new-tool.ts
git commit -m "feat: add new_tool implementation"

git add README.md
git commit -m "docs: document new_tool usage and examples"
```

## Development Workflow

### Adding a New Tool to Existing Server

1. **Create types** (if needed):
   ```bash
   # Edit src/utils/types.ts
   git add src/utils/types.ts
   git commit -m "feat: add types for new_tool"
   ```

2. **Create utilities** (if needed):
   ```bash
   # Create helper functions
   git add src/utils/new-helper.ts
   git commit -m "feat: add helper utilities for new_tool"
   ```

3. **Implement the tool**:
   ```bash
   git add src/tools/new-tool.ts
   git commit -m "feat: add new_tool implementation"
   ```

4. **Write tests**:
   ```bash
   git add __tests__/tools/new-tool.test.ts
   git commit -m "test: add comprehensive tests for new_tool"
   ```

5. **Wire into server**:
   ```bash
   git add src/index.ts
   git commit -m "feat: register new_tool in MCP server"
   ```

6. **Add documentation**:
   ```bash
   git add README.md
   git commit -m "docs: add new_tool usage examples"
   ```

7. **Run tests and fix issues**:
   ```bash
   npm test
   # If tests fail, fix and commit
   git add src/tools/new-tool.ts
   git commit -m "fix: address edge case discovered in tests"
   ```

8. **Build and verify**:
   ```bash
   npm run build
   # Test manually in MCP client
   ```

### Creating a New MCP Server

Follow the same incremental approach:

1. Directory structure → commit
2. package.json → commit
3. tsconfig.json → commit
4. Types → commit
5. Each utility → commit
6. Each tool → commit
7. Server entry point → commit
8. README → commit
9. Update root README → commit

## Code Style Guidelines

### File Organization

- One tool per file in `src/tools/`
- Group related utilities in `src/utils/`
- Export schemas and implementation functions together
- Keep files under 300 lines (split if larger)

### Naming Conventions

- **Files**: `kebab-case.ts` (e.g., `changelog-update.ts`)
- **Functions**: `camelCase` (e.g., `parseChangelog`)
- **Types/Interfaces**: `PascalCase` (e.g., `ChangelogEntry`)
- **Constants**: `UPPER_SNAKE_CASE` (e.g., `VALID_SECTIONS`)
- **MCP Tools**: `snake_case` (e.g., `changelog_update`)

### Imports

Use explicit imports with `.js` extension (required for ESM):

```typescript
import { parseChangelog } from '../utils/changelog-parser.js';
import type { ChangelogEntry } from '../utils/types.js';
```

### Function Documentation

```typescript
/**
 * Parse a CHANGELOG.md file into structured data
 * 
 * @param content - Raw markdown content of the changelog
 * @returns Structured changelog object with entries and sections
 * @throws Error if content is malformed
 */
export function parseChangelog(content: string): ChangelogStructure {
  // Implementation
}
```

## Testing Guidelines

### Automated Testing with Playwright (REQUIRED)

**All code MUST have tests. No PRs without tests.**

```bash
# Run all tests
npm test

# Run tests in watch mode
npm test -- --watch

# Run specific test file
npm test __tests__/tools/my-tool.test.ts

# Run with coverage
npm test -- --coverage
```

**Test Structure:**

```typescript
import { test, expect, describe } from '@playwright/test';

describe('tool_name', () => {
  describe('happy path', () => {
    test('should process valid input correctly', async () => {
      const result = await myTool({ param: 'valid' });
      expect(result.success).toBe(true);
    });
  });

  describe('error handling', () => {
    test('should reject invalid input', async () => {
      const result = await myTool({ param: '' });
      expect(result.success).toBe(false);
    });
  });

  describe('edge cases', () => {
    test('should handle empty values', async () => {
      // Test edge case
    });

    test('should handle extreme values', async () => {
      // Test edge case
    });
  });
});
```

**Coverage Requirements:**
- Minimum 80% code coverage
- 100% coverage for critical paths
- All error cases must be tested
- All edge cases must be tested

### Manual Testing (Integration)

After automated tests pass, test manually:

```bash
# Build
npm run build

# Test tool listing
echo '{"jsonrpc":"2.0","id":1,"method":"tools/list"}' | node build/index.js

# Test in actual MCP client
# 1. Update ~/.claude/settings.json or equivalent
# 2. Restart client
# 3. Invoke tool via natural language
```

## Build and Compilation

### Before Committing Code

1. **Ensure it builds**:
   ```bash
   npm run build
   ```

2. **Check for TypeScript errors**:
   ```bash
   npx tsc --noEmit
   ```

3. **Test manually** (see Testing Guidelines)

### Build Artifacts

- `build/` directory is gitignored
- Never commit compiled `.js` files
- Never commit `node_modules/`
- Package-lock.json is gitignored (npm install will regenerate)

## Dependencies

### Adding New Dependencies

1. **Add dependency**:
   ```bash
   npm install new-package
   git add package.json
   git commit -m "chore: add new-package dependency"
   ```

2. **If using in code**:
   ```bash
   # Make code changes
   git add src/tools/tool-using-package.ts
   git commit -m "feat: use new-package in tool implementation"
   ```

### Dependency Guidelines

- Minimize dependencies
- Only use well-maintained packages
- Prefer built-in Node.js APIs when possible
- Document why each dependency is needed

## Common Pitfalls to Avoid

### ❌ DON'T

- Make large, monolithic commits
- Use `git add .` without reviewing changes
- Commit build artifacts
- Skip documentation updates
- Skip writing tests (NO EXCEPTIONS)
- Use `any` types without justification
- Ignore TypeScript errors
- Make breaking changes without versioning
- Commit directly to main without testing
- Deploy code without running tests
- Merge PRs with failing tests

### ✅ DO

- Make small, focused commits
- Review changes before committing (`git diff`)
- Keep build/ directory clean
- Document every feature and change
- Write tests for every feature (MANDATORY)
- Run tests before committing (`npm test`)
- Use proper TypeScript types
- Fix all TypeScript errors before committing
- Follow semantic versioning
- Test manually in MCP client after automated tests pass
- Ensure 80%+ code coverage

## MCP-Specific Guidelines

### Tool Schema Design

```typescript
export const toolSchema = {
  name: 'tool_name',  // snake_case
  description: 'Clear, concise description of what this tool does',
  inputSchema: {
    type: 'object',
    properties: {
      required_param: {
        type: 'string',
        description: 'Clear description with examples',
      },
      optional_param: {
        type: 'boolean',
        description: 'What this does',
        default: false,
      },
    },
    required: ['required_param'],
  },
};
```

### Tool Implementation Patterns

```typescript
export async function toolImplementation(args: {
  required_param: string;
  optional_param?: boolean;
}): Promise<{ summary: string; data?: any }> {
  try {
    // Validate inputs
    if (!args.required_param) {
      return { summary: '❌ Error: required_param is missing' };
    }

    // Do the work
    const result = await doWork(args.required_param);

    // Return user-friendly response
    return {
      summary: `✅ Success! Processed ${result.count} items`,
      data: result,
    };
  } catch (error) {
    return {
      summary: `❌ Error: ${error instanceof Error ? error.message : String(error)}`,
    };
  }
}
```

### Response Format

- Use emoji sparingly (✅, ❌, ⚠️ for status)
- Provide clear, actionable messages
- Include relevant data in responses
- Use markdown formatting for readability

## File Headers

Add a comment header to main files:

```typescript
/**
 * Tool: Description of what this tool does
 * 
 * Part of the MCP Servers collection
 * https://github.com/your-repo/mcp-servers
 */
```

## Git Branch Strategy

- `main` - Stable, production-ready code
- `feature/*` - New features (e.g., `feature/add-diff-tool`)
- `fix/*` - Bug fixes (e.g., `fix/symlink-resolution`)
- `docs/*` - Documentation updates (e.g., `docs/improve-readme`)

**Work in branches, merge to main via pull requests (if using GitHub).**

## Version Management

Follow [Semantic Versioning](https://semver.org/):

- `MAJOR.MINOR.PATCH`
- MAJOR: Breaking changes
- MINOR: New features (backwards compatible)
- PATCH: Bug fixes

Update version in `package.json` and commit:
```bash
git add package.json
git commit -m "chore: bump version to 1.2.0"
git tag v1.2.0
```

## Pre-Commit Checklist

Before every commit, verify:

- [ ] Code compiles (`npm run build`)
- [ ] No TypeScript errors (`npx tsc --noEmit`)
- [ ] All tests pass (`npm test`)
- [ ] Code coverage meets minimum (80%)
- [ ] Tested manually in MCP client (for features)
- [ ] Documentation updated (if needed)
- [ ] Commit message follows conventional format
- [ ] Commit is small and focused (one logical change)
- [ ] No console.log() or debugging code left in
- [ ] No commented-out code (remove or document why)

## Parallel Work (CRITICAL for Multiple Agents)

**When multiple agents are working simultaneously:**

1. **Read `PARALLEL_WORK_GUIDELINES.md` BEFORE starting work**
2. **Check `.file-locks` for locked files**
3. **Announce your work** - Communicate what you're working on
4. **Work in isolation** - Use separate branches and files
5. **Coordinate on shared files** - Only one agent per file
6. **Lock high-conflict files** when modifying:
   - Add entry to `.file-locks`
   - Format: `filepath|agent-id|timestamp|reason`
   - Remove lock when done
7. **Pull frequently** - Every 15-30 minutes
8. **Serialize merges** - One agent merges at a time

**High-conflict files requiring locks:**
- `src/index.ts` (MCP entry point)
- `package.json` (dependencies)
- `README.md` (main docs)
- `src/utils/types.ts` (shared types)

**Branch naming for parallel work:**
```
agent-{id}/{type}/{description}
Example: agent-a/feature/add-diff-tool
```

## Questions or Clarifications?

If you're unsure about:
- How to structure something
- Whether to commit now or later
- How to write a commit message
- **Whether another agent is working on the same file**
- **How to coordinate with other agents**

**Ask the user first.** Don't guess.

## Additional Guardrails

### 7. Security (MANDATORY)

- Never commit secrets, API keys, or credentials
- Use environment variables for sensitive data
- Validate all user inputs
- Sanitize file paths to prevent directory traversal
- Use parameterized queries if interacting with databases
- Keep dependencies up to date for security patches

### 8. Performance (IMPORTANT)

- Avoid unnecessary file I/O operations
- Use streaming for large files
- Implement proper caching where appropriate
- Don't block the event loop with synchronous operations
- Profile performance-critical code

### 9. Accessibility & User Experience (IMPORTANT)

- Provide clear, actionable error messages
- Include progress indicators for long-running operations
- Support graceful degradation
- Handle edge cases (empty inputs, missing files, etc.)
- Provide helpful suggestions when operations fail

### 10. Code Quality (MANDATORY)

- No code duplication (DRY principle)
- Functions should do one thing well (Single Responsibility)
- Maximum function length: 50 lines (split if longer)
- Maximum file length: 300 lines (split if longer)
- No magic numbers (use named constants)
- No nested ternaries (use if-else instead)
- Prefer early returns over deep nesting

### 11. CI/CD Integration (FUTURE)

When setting up CI/CD:
- All tests must pass before merge
- Code coverage gates (minimum 80%)
- TypeScript compilation must succeed
- Linting must pass
- No security vulnerabilities allowed

### 12. Logging and Debugging (IMPORTANT)

- Use `console.error()` for stderr (MCP servers use stdout for protocol)
- Remove `console.log()` before committing
- Add meaningful error messages with context
- Log errors with enough detail for debugging

Example:
```typescript
try {
  await operation();
} catch (error) {
  console.error('Failed to process changelog:', {
    operation: 'changelog_update',
    error: error instanceof Error ? error.message : String(error),
    context: { file: changelogPath }
  });
  throw error;
}
```

## Summary for AI Agents

When working on this project:

1. **Make incremental commits** - This is the #1 rule
2. **Write comprehensive tests** - No code without tests (using Playwright)
3. **Follow conventional commits** - Proper commit messages
4. **Use TypeScript strictly** - No shortcuts, no `any` types
5. **Follow the structure** - Don't invent new patterns
6. **Document everything** - Code + usage examples + tests
7. **Test before committing** - Automated tests + manual verification
8. **Security first** - Never commit secrets, validate inputs
9. **Ask if unsure** - Don't guess

**Remember:** 
- Small commits = easy to review = easy to revert = professional history
- No code without tests = reliable software = fewer bugs
- Tests first = better design = clearer requirements
